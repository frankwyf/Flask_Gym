[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = [
  ".venv",
  "instance",
  "Loggings",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N"]
ignore = ["N802", "N803", "N806"]

[tool.ruff.lint.isort]
known-first-party = ["app", "config", "scripts"]
